<nav class="navbar navbar-light bg-white shadow-sm mb-4 px-4 py-3">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <a href="{{ path('home') }}" class="navbar-brand fw-semibold" style="color: #001a70; font-family: 'Poppins', sans-serif; font-size: 20px;">
            Accueil
        </a>
        <span class="text-muted small d-flex align-items-center" style="font-family: 'Inter', sans-serif;">
            <img src="{{ asset('images/logo-blog.png') }}" alt="Logo" style="height: 36px; margin-right: 6px;">
            Think. Build. Share.
        </span>
        <div class="nav-links d-flex gap-3">
            <a href="{{ path('post_list') }}" class="nav-link-custom">Articles</a>
            <a href="#" class="nav-link-custom">Présentation</a>
            <a href="#" class="nav-link-custom">Contact</a>
        </div>
    </div>
</nav>
